<%= form_with(model: [ @article, @article.comments.build ], local: true) do |form| %>
  <% react_component('article', {article: @article, comments: @article.comments}) %>
<% end %>

<%= link_to 'Edit', edit_article_path(@article) %> |
<%= link_to 'Back', articles_path %>
